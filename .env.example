# =====================================================
# Sendwe SOS — Dashboard hôpital (Vite)
# Copier vers .env pour le dev local, OU définir dans
# Cloudflare Pages → Settings → Environment variables.
# =====================================================

# URL publique du backend (Railway) — utilisée par le navigateur.
# ⚠️ NE PAS utiliser l'adresse interne *.railway.internal (inaccessible depuis un navigateur).
# Optionnel en dev (proxy Vite → localhost:8000).
# Exemple : https://sendwe-sos-backend.up.railway.app
VITE_API_URL=
